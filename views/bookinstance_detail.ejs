<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <!-- Navigation Layout-->
        <%- include('layout') -%>

        <div class="col-sm-10 mt-3">
          <h1>ID: <%= bookinstance._id %></h1>

          <p>
            <strong>Title: </strong>
            <a href="<%= bookinstance.book.url %>"
              ><%= bookinstance.book.title %></a
            >
          </p>

          <p><strong>Imprint: </strong><%= bookinstance.imprint %></p>

          <p>
            <strong>Status: </strong>
            <% if (bookinstance.status=='Available') { %>
            <small class="text-success"><%= bookinstance.status %></small>
            <% } else if(bookinstance.status=='Maintenance') { %>
            <small class="text-danger"><%= bookinstance.status %></small>
            <% } else { %>
            <small class="text-warning"><%= bookinstance.status %></small>
            <% } %>
          </p>

          <% if(bookinstance.status!='Available'){ %>
          <p><strong>Due back: </strong><%= bookinstance.due_back %></p>
          <% } %>

          <div class="mt-5 mb-5">
            <a class="me-3" href="<%= bookinstance.url  %>/update"
              >Update Book Instance</a
            >
            <a href="<%= bookinstance.url %>/delete" class="link-danger"
              >Delete Book Instance</a
            >
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
